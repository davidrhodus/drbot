[package]
name = "drbot-buffer"
version.workspace = true
edition.workspace = true
description = "Buffer utilities for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
