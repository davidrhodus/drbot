[package]
name = "drbot-unit"
version.workspace = true
edition.workspace = true
description = "Unit type utilities for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
