[package]
name = "drbot-cast"
version.workspace = true
edition.workspace = true
description = "Type casting utilities for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
