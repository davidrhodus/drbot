[package]
name = "drbot-either"
version.workspace = true
edition.workspace = true
description = "Either type utilities for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
