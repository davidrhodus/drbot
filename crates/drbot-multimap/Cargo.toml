[package]
name = "drbot-multimap"
version.workspace = true
edition.workspace = true
description = "One-to-many map for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
