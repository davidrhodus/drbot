[package]
name = "drbot-lazy"
version.workspace = true
edition.workspace = true
description = "Lazy evaluation for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
