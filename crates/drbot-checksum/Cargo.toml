[package]
name = "drbot-checksum"
version.workspace = true
edition.workspace = true
description = "Checksum calculation for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
