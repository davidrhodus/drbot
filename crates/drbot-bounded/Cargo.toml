[package]
name = "drbot-bounded"
version.workspace = true
edition.workspace = true
description = "Bounded numeric types for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
