[package]
name = "drbot-cmp"
version.workspace = true
edition.workspace = true
description = "Comparison utilities for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
