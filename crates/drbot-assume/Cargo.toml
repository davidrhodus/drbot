[package]
name = "drbot-assume"
version.workspace = true
edition.workspace = true
description = "Assumption checking for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
