[package]
name = "drbot-atomic-ext"
version.workspace = true
edition.workspace = true
description = "Atomic type extensions for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
