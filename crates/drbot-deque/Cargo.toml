[package]
name = "drbot-deque"
version.workspace = true
edition.workspace = true
description = "Double-ended queue utilities for drbot"

[dependencies]
thiserror.workspace = true

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
